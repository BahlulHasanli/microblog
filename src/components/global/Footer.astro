<!-- Footer -->
<footer class="max-w-3xl mx-auto text-base-800 bg-base-50 p-4 sm:p-6 md:p-8 rounded-lg sm:rounded-xl lg:p-10 my-6 sm:my-8 md:my-10">
  <div class="mx-auto max-w-6xl flex justify-center flex-col items-center gap-3 sm:gap-5">
    <ul class="flex flex-wrap justify-center gap-2 sm:gap-4 md:gap-6">
      <li><a href="/about" class="text-[11px] sm:text-[12px] md:text-[13px] hover:text-base-700/80 font-semibold">HAQQINDA</a></li>
      <li><a href="/contact" class="text-[11px] sm:text-[12px] md:text-[13px] hover:text-base-700/80 font-semibold">ƏLAQƏ</a></li>
      <li><a href="/adv" class="text-[11px] sm:text-[12px] md:text-[13px] hover:text-base-700/80 font-semibold">REKLAM</a></li>
      <li><a href="/vacancy" class="text-[11px] sm:text-[12px] md:text-[13px] hover:text-base-700/80 font-semibold">VAKANSİYA</a></li>
      <li><a href="/squad" class="text-[11px] sm:text-[12px] md:text-[13px] hover:text-base-700/80 font-semibold">SQUAD</a></li>
      <li class="relative">
        <button
        id="random-post-btn"
        aria-label="Təsadüfi post"
        class="text-lg cursor-pointer absolute -top-2 sm:text-xl md:text-3xl hover:text-base-700 font-semibold"
        title="Təsadüfi"
      >
        🎲
      </button></li>
      <!-- <li><a href="/premieres" class="text-[11px] sm:text-[12px] md:text-[13px] hover:text-base-700/80 font-semibold">BİR ŞEY VAR?</a></li> -->
    </ul>

    <p class="text-[10px] sm:text-[11px] md:text-[12px] text-base-700 text-center cursor-default px-2">
      1999 - 2025 "The Doxsandoqquz". Saytdakı materiallarda istifadə olunan bütün şəkillər, videolar və mətnlərin hüququ, müəlliflərin birbaşa özlərinə məxsusdur.
    </p>

    <div class="bg-base-200/40 text-base-700 text-[10px] sm:text-[11px] md:text-[12px] px-3 sm:px-5 py-1 rounded-full cursor-default">
      Burada <span class="font-semibold">+18</span> yaşından aşağı olan şəxslər üçün nəzərdə tutulmayan materiallar ola bilər.
    </div>
  </div>
</footer>

<script>
  import { navigate } from 'astro:transitions/client';

  function setupRandomButton() {
    const randomBtn = document.getElementById('random-post-btn');
    if (!randomBtn) return;

    randomBtn.addEventListener('click', async () => {
      try {
        const response = await fetch('/api/posts/random');
        if (response.ok) {
          const data = await response.json();
          if (data.slug) {
            navigate(`/posts/${data.slug}`);
          }
        }
      } catch (error) {
        console.error('Random məqalə yükləməsi xətası:', error);
      }
    });
  }

  setupRandomButton();
  document.addEventListener('astro:after-swap', setupRandomButton);
</script>